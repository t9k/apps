apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "open-webui.fullname" . }}
  labels:
    {{- include "open-webui.labels" . | nindent 4 }}
spec:
  gateways:
  - project-gateway
  hosts:
  - '*'
  http:
  - match:
    - uri:
        prefix: /t9k/app/open-webui/{{ .Release.Namespace }}/{{ .Release.Name }}/
    rewrite:
      uri: /
    route:
    - destination:
        host: {{ include "open-webui.fullname" . }}
        port:
          number: {{ .Values.service.port }}
