apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "gpt-researcher.fullname" . }}
  labels:
    {{- include "gpt-researcher.labels" . | nindent 4 }}
spec:
  gateways:
  - project-gateway
  hosts:
  - '*'
  http:
  - match:
    - uri:
        prefix: /t9k/app/gpt-researcher/{{ .Release.Namespace }}/{{ .Release.Name }}/
    rewrite:
      uri: /
    route:
    - destination:
        host: {{ include "gpt-researcher.fullname" . }}
        port:
          number: {{ .Values.service.port }}
